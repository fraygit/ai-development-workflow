# Task 1.2 — GitHub Actions Workflow (jira-implement.yml)

**Story:** 1.0 — Jira → Implement → PR  
**Skill:** senior-devops  
**Priority:** P0 — depends on Tasks 0.1 and 1.1

---

## What to do

Write `.github/workflows/jira-implement.yml` in the POC repo. This workflow:
1. Accepts a trigger from `repository_dispatch` (Jira bridge) or `workflow_dispatch` (manual test).
2. Checks out the POC repo (for skill files) and clones the sample app repo (the target) separately using a PAT.
3. Pre-fetches the Jira ticket.
4. Creates a feature branch in the sample app repo.
5. Runs Claude Code CLI with the `nodejs-developer` skill, working inside the sample app directory.
6. Pushes the branch and opens a PR — both using the PAT, not `GITHUB_TOKEN`.

The two repos serve distinct roles: the **POC repo** provides the skill; the **sample app repo** is what gets modified.

---

## File to Create

**Path in POC repo:** `.github/workflows/jira-implement.yml`

```yaml
name: Jira — Implement Ticket

on:
  repository_dispatch:
    types: [jira-ticket-assigned]
  workflow_dispatch:
    inputs:
      jira_ticket_id:
        description: 'Jira ticket ID (e.g. POC-1)'
        required: true
        type: string

jobs:
  implement:
    runs-on: ubuntu-latest
    # No special permissions needed — all sample repo operations use SAMPLE_NODEJS_PAT

    steps:
      - name: Checkout POC repo (for skill files)
        uses: actions/checkout@v4
        with:
          path: poc

      - name: Resolve Jira ticket ID
        id: ticket
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            echo "id=${{ github.event.client_payload.ticket_id }}" >> $GITHUB_OUTPUT
          else
            echo "id=${{ inputs.jira_ticket_id }}" >> $GITHUB_OUTPUT
          fi

      - name: Clone sample Node.js app
        env:
          SAMPLE_NODEJS_PAT: ${{ secrets.SAMPLE_NODEJS_PAT }}
        run: |
          git clone \
            https://x-access-token:$SAMPLE_NODEJS_PAT@github.com/fraygit/ai-development-workflow-sample-nodejs.git \
            target

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install sample app dependencies
        working-directory: target
        run: npm ci

      - name: Fetch Jira ticket
        id: jira
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
        run: |
          TICKET_ID="${{ steps.ticket.outputs.id }}"
          AUTH=$(echo -n "$JIRA_USER_EMAIL:$JIRA_API_TOKEN" | base64 -w0)

          RESPONSE=$(curl -sf \
            -H "Authorization: Basic $AUTH" \
            -H "Content-Type: application/json" \
            "$JIRA_BASE_URL/rest/api/3/issue/$TICKET_ID")

          SUMMARY=$(echo "$RESPONSE" | jq -r '.fields.summary')
          DESCRIPTION=$(echo "$RESPONSE" | jq -r '
            [.fields.description.content[]?.content[]?
              | select(.type == "text") | .text] | join(" ")' 2>/dev/null || echo "")

          echo "summary=$SUMMARY" >> $GITHUB_OUTPUT
          {
            echo "description<<EOF"
            echo "$DESCRIPTION"
            echo "EOF"
          } >> $GITHUB_OUTPUT

      - name: Create feature branch
        id: branch
        working-directory: target
        run: |
          BRANCH="feature/${{ steps.ticket.outputs.id }}-ai-implementation"
          git checkout -b "$BRANCH"
          git config user.name "AIDevFlow Bot"
          git config user.email "bot@aidevflow.io"
          echo "name=$BRANCH" >> $GITHUB_OUTPUT

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Run nodejs-developer skill
        working-directory: target
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          JIRA_TICKET_ID: ${{ steps.ticket.outputs.id }}
          JIRA_SUMMARY: ${{ steps.jira.outputs.summary }}
          JIRA_DESCRIPTION: ${{ steps.jira.outputs.description }}
        run: |
          claude --no-interactive \
            --allowedTools "Read,Edit,Write,Bash,Glob,Grep" \
            -p "$(cat ../poc/.claude/skills/nodejs-developer/SKILL.md)"

      - name: Check for changes
        id: changes
        working-directory: target
        run: |
          if git diff --quiet && git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Fail if no changes made
        if: steps.changes.outputs.has_changes == 'false'
        run: |
          echo "Claude made no file changes. Check the skill output above."
          exit 1

      - name: Push branch to sample app repo
        working-directory: target
        env:
          SAMPLE_NODEJS_PAT: ${{ secrets.SAMPLE_NODEJS_PAT }}
        run: |
          git push \
            https://x-access-token:$SAMPLE_NODEJS_PAT@github.com/fraygit/ai-development-workflow-sample-nodejs.git \
            "${{ steps.branch.outputs.name }}"

      - name: Create Pull Request
        working-directory: target
        env:
          GH_TOKEN: ${{ secrets.SAMPLE_NODEJS_PAT }}
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
        run: |
          TICKET_ID="${{ steps.ticket.outputs.id }}"
          BRANCH="${{ steps.branch.outputs.name }}"

          gh pr create \
            --title "[$TICKET_ID] AI Implementation" \
            --body "## AI-generated implementation

**Jira ticket:** $JIRA_BASE_URL/browse/$TICKET_ID

This PR was generated by Claude Code (nodejs-developer skill). Review all changes carefully before merging.

> ⚠️ **Auto-merge is disabled.** A human must review and approve this PR before it can be merged." \
            --head "$BRANCH" \
            --base main \
            --repo fraygit/ai-development-workflow-sample-nodejs
```

---

## Acceptance Criteria

- [x] Workflow file exists at `.github/workflows/jira-implement.yml` in the POC repo.
- [x] POC repo checked out at `./poc/` — skill files referenced as `../poc/.claude/skills/...`.
- [x] Sample app cloned at `./target/` using `SAMPLE_NODEJS_PAT` (not `GITHUB_TOKEN`).
- [x] Branch created inside `./target/`, not the POC repo.
- [x] Git identity configured in `./target/` before Claude runs.
- [x] Claude invoked with `working-directory: target` so it edits the sample app files.
- [x] Push and PR both use `SAMPLE_NODEJS_PAT` — `gh pr create` includes `--repo fraygit/ai-development-workflow-sample-nodejs`.
- [x] Workflow fails clearly if Claude makes no changes.

**Manual test sequence:**
- [x] Complete Task 0.1 (sample app created, `SAMPLE_NODEJS_PAT` configured as a secret).
- [x] Write and commit a Jira ticket (e.g. "Add a `greet(name)` function to `src/index.js`").
- [x] Go to GitHub → POC repo → Actions → `Jira — Implement Ticket` → Run workflow → enter ticket ID.
- [x] Workflow completes green.
- [x] Open `ai-development-workflow-sample-nodejs` → Pull Requests → confirm PR exists with Claude's changes.
- [x] Review the diff — changes should match the ticket description.

---

## First Test Run

Before triggering the workflow, make sure these secrets are set in `ai-development-workflow-poc` → **Settings → Secrets and variables → Actions** (in addition to `SAMPLE_NODEJS_PAT` from Task 0.1):

| Secret | Where to get it |
|---|---|
| `ANTHROPIC_API_KEY` | [console.anthropic.com](https://console.anthropic.com) → API Keys |
| `JIRA_BASE_URL` | Your Jira instance URL, e.g. `https://yourorg.atlassian.net` |
| `JIRA_API_TOKEN` | Go to [id.atlassian.com/manage-profile/security/api-tokens](https://id.atlassian.com/manage-profile/security/api-tokens) → **Create API token** |
| `JIRA_USER_EMAIL` | The email address your Jira API token belongs to |

Once all secrets are set:

1. Create a Jira ticket with a small, unambiguous task — e.g.:
   > **Summary:** Add a `greet(name)` function  
   > **Description:** Add and export a `greet(name)` function to `src/index.js` that returns `Hello, {name}!`. Add a test for it in `test/index.test.js`.

2. Go to [github.com/fraygit/ai-development-workflow-poc](https://github.com/fraygit/ai-development-workflow-poc) → **Actions** → **Jira — Implement Ticket** → **Run workflow** → enter the ticket ID → **Run workflow**.

3. Watch the workflow run. It will:
   - Clone `ai-development-workflow-sample-nodejs`
   - Fetch the Jira ticket
   - Create branch `feature/<ticket-id>-ai-implementation`
   - Run Claude Code with the `nodejs-developer` skill
   - Push the branch and open a PR in the sample app repo

4. Open [github.com/fraygit/ai-development-workflow-sample-nodejs](https://github.com/fraygit/ai-development-workflow-sample-nodejs) → **Pull Requests** → confirm the PR is there and the diff matches the ticket description.

---

## Notes

- **Why `x-access-token` in the clone URL?** This is the standard way to embed a PAT in a git clone URL for GitHub. The token is masked in the Actions log. The push step uses the same pattern — no separate `git remote set-url` needed.
- **`working-directory: target`** on the Claude step is what ensures Claude edits the sample app, not the POC repo. All file tool calls from Claude resolve relative to that directory.
- **`--repo` flag on `gh pr create`** is required because the `gh` CLI detects the current git remote to determine where to create the PR, but we want to be explicit since there are two repos in the workspace.
- **No `contents: write` permission at the job level** — the job-level permissions only affect `GITHUB_TOKEN` (the POC repo token). Since we never write to the POC repo in this workflow, no elevated permissions are needed.

---

## Fixes Applied During Testing

These issues were discovered and fixed during the first end-to-end test run. Documented here so they are not repeated when building similar workflows.

**Fix 1 — YAML indentation error in PR body**
The original `gh pr create --body "..."` spanned multiple lines with content at column 0 inside a `run: |` block. YAML requires all content in a literal block scalar to stay at or above the block's indentation level. Lines at column 0 broke the YAML parse, causing the workflow to show by filename in the Actions tab with no "Run workflow" button.
→ **Fix:** build the PR body with `printf` into a variable on a single properly-indented line.

**Fix 2 — `npm ci` fails without `package-lock.json`**
The "Install sample app dependencies" step ran `npm ci` against the sample app which has no dependencies and therefore no `package-lock.json`. `npm ci` requires a lock file to exist.
→ **Fix:** removed the step entirely. The sample app has no dependencies; Node's built-in test runner needs no install.

**Fix 3 — `--no-interactive` is not a valid Claude Code CLI flag**
The `--no-interactive` flag does not exist. Running it produced `error: unknown option '--no-interactive'` and exit code 1.
→ **Fix:** replaced with `-p` (print/non-interactive mode) and added `--dangerously-skip-permissions` to bypass tool permission prompts in CI.

**Fix 4 — Passing SKILL.md via `$(cat)` broke Claude invocation**
The skill file content contains YAML frontmatter, triple-backtick code blocks, and multi-line bash examples. When passed as `"$(cat skill.md)"`, the structure was misinterpreted — Claude outputted the skill content as text rather than executing it, then exited with code 1.
→ **Fix:** replaced with an inline `printf` prompt that injects the Jira context (`$JIRA_TICKET_ID`, `$JIRA_SUMMARY`, `$JIRA_DESCRIPTION`) directly as `%s` arguments. Single-quoted format string prevents shell expansion inside.

**Fix 5 — "Check for changes" missed committed changes**
The check used `git diff --quiet && git diff --staged --quiet`, which only detects uncommitted changes. The Claude prompt tells Claude to `git add -A && git commit`, so after Claude runs the working tree is clean. The check reported "no changes" even when Claude had committed successfully.
→ **Fix:** added `git rev-list origin/main..HEAD --count` to detect new commits on the feature branch alongside the existing uncommitted-change check.
